# Stop process specified time  
`sleep`　is stopping proccess command.  
`-s` means seconds, `-m` means minutes, `-h` means hours, `-d` means days.  
If you stop process 5seconds, you write in your code `sleep -5s`.  
