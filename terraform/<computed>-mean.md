# terraform plan results `<computed>` mean  
Executed `terraform plan`, you may see `<computed>` in execute results.  
`<computed>` means the value is undetermined until `terraform apply` is executed.  
